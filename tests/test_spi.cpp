#include <gtest/gtest.h>

// Test case 1: SPI send data
TEST(SPITests, SendData)
{
    // Test logic for SPI send data
    ASSERT_EQ(1, 1);
}

// Test case 2: SPI receive data
TEST(SPITests, ReceiveData)
{
    // Test logic for SPI receive data
    ASSERT_EQ(1, 1);
}

